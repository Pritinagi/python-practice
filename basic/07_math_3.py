import math 
# c=2pier
radius=float(input('enter the radius of circele '))
circumference = 2* math.pi *radius
print(f"the circumference is : {round(circumference) }")

area= math.pi*pow(radius,2)
print(f"the area is : {round(area,2) }")
